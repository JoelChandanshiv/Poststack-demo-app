terraform {
  backend "s3" {
    bucket       = "<STATE_BUCKET_NAME>"
    key          = "platform/drift-detection/terraform.tfstate"
    region       = "ap-south-2"
    encrypt      = true
    kms_key_id   = "<KMS_KEY_ARN>"
    use_lockfile = true
  }
}
